import { Layout } from "../components/layout/Layout";
import { Hero } from "../features/home/Hero";

export const Home = () => {
    return (
        <Layout>
            <Hero />
        </Layout>
    );
};